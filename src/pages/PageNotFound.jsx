function PageNotFound() {
  return (
    <div
      className="container"
      style={{
        fontSize: "20px",
        color: "white",
        fontWeight: "800",
        display: "grid",
        placeItems: "center",
        alignItems: "center",
      }}
    >
      The page that you are looking for does not exist !
    </div>
  );
}

export default PageNotFound;
